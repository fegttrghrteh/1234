"use strict";

// Дополнительный вариант практики для запуска в браузере.
// Подключите этот файл к index.html и выполняйте задачи по очереди.
// prompt() возвращает строку: числовые значения преобразуйте через Number().
// confirm() сразу возвращает true или false.

// Задача 1. Сравнение двух чисел — пример полностью
const a = Number(prompt("Задача 1. Введите первое число:"));
const b = Number(prompt("Задача 1. Введите второе число:"));

let comparisonResult;

if (a > b) {
  comparisonResult = ">";
} else if (a < b) {
  comparisonResult = "<";
} else {
  comparisonResult = "=";
}

alert(`Результат сравнения: ${comparisonResult}`);

// Задача 2. Проверка диапазона
// const value = Number(prompt("Задача 2. Введите проверяемое значение:"));
const min = Number(prompt("Задача 2. Введите нижнюю границу (min):"));
const max = Number(prompt("Задача 2. Введите верхнюю границу (max):"));

const inRange = value >= min && value <= max;
alert(`Значение входит в диапазон: ${inRange}`);

// Задача 3. Проверка доступа
// TODO:
// 1) получить age через prompt();
// 2) получить hasTicket через confirm();
// 3) получить isBlocked через confirm();
// 4) проверить все три условия;
// 5) вывести результат через alert().

// Задача 4. Оценка по баллам
// TODO:
// 1) получить score через prompt();
// 2) обработать некорректный диапазон;
// 3) определить оценку через if / else if / else;
// 4) вывести оценку.

// Задача 5. Процент скидки
// TODO:
// 1) получить total;
// 2) определить процент скидки по условию;
// 3) вывести процент.

// Задача 6. Тип дня недели
// TODO:
// 1) получить day от 1 до 7;
// 2) использовать switch;
// 3) для 1..5 вывести "Рабочий день";
// 4) для 6..7 вывести "Выходной день";
// 5) остальные значения обработать через default.
